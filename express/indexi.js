// Joining path with path join

const path = require("path");
const folderName = "./indexi";
const fileName = "./indexi.txt";
const fullPath = path.join(folderName, fileName);
console.log(fullPath);
var path = 'indexi/indexi.txt';
